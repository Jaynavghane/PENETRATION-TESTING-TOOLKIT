# Tests

Add unit tests here for the pentoolkit modules.

## Suggested Tests

- **Port Scanner**: Test against localhost closed port
- **Brute-Forcer**: Test against a local mock HTTP server with Basic Auth
- **CLI**: Test argument parsing and command routing

## Running Tests

```bash
python -m pytest tests/
```
